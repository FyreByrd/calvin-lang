let a = true; // bool
let b = 0.1;  // r64
let c: i16 = 1_000; // i16
let d = 1.0 + 2.0i; // x64
let e = "Hello, World!"; // string
let f = 'aaaa/**/aaaaa//aaaaaaaa'; // embed what would be comments in string
let g = a = b; // never
let h: u8 = 1 + 1; // u8
let i = (x * y) + b; // unknown
let j = x * y + b++; // unknown
let k = x * (y + ++b); // unknown
let l = x and ++y++; // unknown
let m = x or y; // unknown
let n = x in y; // unknown
let o = not (x in y); // unknown
let p = (not /* y */ x) in y /**/; // unknown
let q = not x in y; // This should be parsed the same as p // unknown
{
    let q = 1; // should warn
    q += 30;
    let r = 2;
}
;;;
let r = 2; // should not warn
if (r > 3) {}
elif (let s = 0) {}
else {
    let r = 3; // should warn
    let s = 1;
}
do {}
while (x < 3);
while (y > 4) {

} finally {
    
}
do {} while(true) {}
/**/
/**

*

*/